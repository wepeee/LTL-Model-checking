MATCH (n:BPMNNode {id: "Event_0qdp6v0"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Activity_1f2xj6c"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_13btx37"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_0shoetf"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Activity_1f2xj6c"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_13btx37"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_0shoetf"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Activity_1f2xj6c"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_13btx37"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_0shoetf"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Activity_1f2xj6c"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_13btx37"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_0shoetf"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Activity_1f2xj6c"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_13btx37"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_0shoetf"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Activity_1f2xj6c"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_13btx37"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_0shoetf"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Activity_1f2xj6c"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_13btx37"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_0shoetf"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Activity_1f2xj6c"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_13btx37"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_0shoetf"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Activity_1f2xj6c"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_13btx37"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_0shoetf"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Activity_1f2xj6c"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_13btx37"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_0shoetf"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Activity_1f2xj6c"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_13btx37"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_0shoetf"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Activity_1f2xj6c"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_13btx37"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_0shoetf"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Activity_1f2xj6c"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_13btx37"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_0shoetf"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Activity_1f2xj6c"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_13btx37"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_0shoetf"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Activity_1f2xj6c"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_13btx37"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_0shoetf"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Activity_1f2xj6c"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_13btx37"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Gateway_0shoetf"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Activity_1f2xj6c"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_loop_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_loop_deadlock: G ( F (G exLoop) -> G (F Final) )";

MATCH (n:BPMNNode {id: "Event_0qdp6v0"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_source_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_source_deadlock: G ( (exXORsplit && ! F (exXORjoin)) -> F (deadlock) )";

MATCH (n:BPMNNode {id: "Activity_1f2xj6c"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_source_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_source_deadlock: G ( (exXORsplit && ! F (exXORjoin)) -> F (deadlock) )";

MATCH (n:BPMNNode {id: "Gateway_13btx37"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_source_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_source_deadlock: G ( (exXORsplit && ! F (exXORjoin)) -> F (deadlock) )";

MATCH (n:BPMNNode {id: "Activity_1ekq4l7"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_source_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_source_deadlock: G ( (exXORsplit && ! F (exXORjoin)) -> F (deadlock) )";

MATCH (n:BPMNNode {id: "Event_048m79m"})
SET n.violated = true,
    n.violated_rules = coalesce(n.violated_rules, []) + "LTL_source_deadlock",
    n.violation_reasons = coalesce(n.violation_reasons, []) + "LTL_source_deadlock: G ( (exXORsplit && ! F (exXORjoin)) -> F (deadlock) )";